"use client"; import {useState} from "react"; import Section from "@/components/ui/Section"; import SectionHeading from "@/components/ui/SectionHeading";
export default function FAQClient({faqs}:{faqs:any[]}){const[open,setOpen]=useState<number|null>(0);return <Section><SectionHeading eyebrow="QUESTIONS" heading="Frequently asked questions."/><div className="mt-10 flex flex-col divide-y divide-[var(--color-border)] border-t border-b border-[var(--color-border)]">{faqs.map((f,i)=>{const yes=open===i;return <div key={f.id??f.question}><button type="button" onClick={()=>setOpen(yes?null:i)} aria-expanded={yes} className="flex w-full items-center justify-between gap-4 py-5 text-left min-h-[44px]"><span className="text-body font-semibold">{f.question}</span><span className={`shrink-0 text-[var(--color-text-secondary)] ${yes?"rotate-45":""}`}>+</span></button>{yes&&<p className="text-body pb-5 text-[var(--color-text-secondary)]">{f.answer}</p>}</div>})}</div></Section>;}
