import Section from "@/components/ui/Section"; import SectionHeading from "@/components/ui/SectionHeading"; import {createClient} from "@/lib/supabase/server"; import {SERVICES} from "@/lib/content";
export default async function ServicesSection(){const s=await createClient();const{data}=await s.from("services").select("*").eq("is_active",true).order("display_order");const rows=data?.length?data:SERVICES.map((x,i)=>({...x,id:String(i)}));return <Section id="services"><SectionHeading eyebrow="WHAT WE DO" heading="Website design and development for Nigerian businesses."/><div className="kaytech-stagger mt-12 grid grid-cols-1 gap-6 sm:grid-cols-2">{rows.map((x:any)=><div key={x.id??x.number} className="kaytech-card-hover rounded-card border border-[var(--color-border)] bg-[var(--color-surface)] p-6"><span className="text-support font-semibold text-[var(--color-text-secondary)]">{x.number}</span><h3 className="text-h3 mt-3">{x.title}</h3><p className="text-body mt-2 text-[var(--color-text-secondary)]">{x.short_description??x.description}</p></div>)}</div></Section>;}
